import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["source.unsplash.com","media.istockphoto.com"], // Allow images from Unsplash
  },
  /* config options here */
};

export default nextConfig;
